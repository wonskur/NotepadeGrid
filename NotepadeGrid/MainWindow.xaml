<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NotepadeGrid.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotepadeGrid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NotepadeGrid">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Button.Content="День" x:Name="BtnDay" Margin="5" Padding="10,5"/>
            <Button Button.Content="Ночь" x:Name="BtnNight" Margin="5" Padding="10,5"/>
            <Button Button.Content="Показать сетку" x:Name="BtnGrid" Margin="5" Padding="10,5"/>
        </StackPanel>
        <Grid>
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" BorderThickness="0">
            
        </TextBox>
        </Grid>
    </Grid>

</Window>
